 a logic function that can simulate all the basic logic operations (AND, OR, NOT ).
The function receives three parameters (input1, input2, Logic Gate Type) and return the
result based on the logic gate type.
a. Input1, Input2, and output of the Logic Function can be in {0,1} or {True, False}
b. Gate Type will be a string {AND, OR, NOT}
c. In the case of NOT gate the output will consider input1 only input1 Input2 Gate Type
